let hotel,fs;_06a‍.x([["create",()=>create],["hotels",()=>hotels],["image",()=>image],["sellerHotels",()=>sellerHotels],["remove",()=>remove],["read",()=>read],["update",()=>update]]);_06a‍.w("../models/hotel",[["default",["hotel"],function(v){hotel=v}]]);_06a‍.w("fs",[["default",["fs"],function(v){fs=v}]]);


       const create = async (req, res) => {
  // console.log("req.fields", req.fields);
  // console.log("req.files", req.files);
  try {
    let fields = req.fields;
    let files = req.files;

    let Hotel = new hotel(fields);
    Hotel.postedBy = req.user._id;
    // handle image
    if (files.image) {
      Hotel.image.data = fs.readFileSync(files.image.path);
      Hotel.image.contentType = files.image.type;
    }
    Hotel.save((err, result) => {
      if (err) {
        _06a‍.g.console.log("saving hotel err =>", err);
        res.status(400).send("Error saving");
      } 
      res.json(result);
    })
  } catch (err) {
    _06a‍.g.console.log(err);
    res.status(400).json({
      err: err.message,
    });
  }
};

       const hotels = async (req, res) => {
  let all = await hotel.find({})
    .limit(24)
    .select("-image.data")
    .populate("postedBy", "_id name")
    .exec();
  // console.log(all);
  res.json(all);
};

       const image = async (req, res) => {
  let Hotel = await hotel.findById(req.params.hotelId).exec();
  if (Hotel && Hotel.image && Hotel.image.data !== null) {
    // res.set("Content-Type", Hotel.image.contentType)
    res.send(Hotel.image.data);
  };
};

       const sellerHotels = async (req, res) => {
  let all = await hotel.find({ postedBy: req.user._id })
    .select("-image.data")
    .populate("postedBy", "_id name")
    .exec();
  _06a‍.g.console.log(all);
  res.send(all);
};

       const remove = async (req, res) => {
  let removed = await hotel.findByIdAndDelete(req.params.hotelId)
    .select("-image.data")
    .exec();
  res.json(removed);
};

       const read = async (req, res) => {
  let Hotel = await hotel.findById(req.params.hotelId)
    .populate("postedBy", "_id name")
    .select("-image.data")
    .exec();
  _06a‍.g.console.log("SINGLE HOTEL", Hotel);
  res.json(Hotel);
};

       const update = async (req, res) => {
  try {
    let fields = req.fields;
    let files = req.files;

    let data = { ...fields };
    
    if (files.image) {
      let image = {}
      image.data = fs.readFileSync(files.image.path);
      image.contentType = "image/jpeg";

      data.image = image;
    }

    let update = await hotel.findByIdAndUpdate(req.params.hotelId, data, {
      new: true,
    })
    //.select(-image.data);
    res.json(update);
  } catch (err) {
    _06a‍.g.console.log(err);
    res.status(400).send("Hotel update failed. Try again.");
  }
};