let expressJwt,hotel;_4da‍.x([["requireSignin",()=>requireSignin],["hotelOwner",()=>hotelOwner]]);_4da‍.w("express-jwt",[["default",["expressJwt"],function(v){expressJwt=v}]]);_4da‍.w("../models/hotel",[["default",["hotel"],function(v){hotel=v}]]);

// req.user
       const requireSignin = expressJwt({
  // secret, exporyDate
  secret: process.env.JWT_SECRET,
  algorithms: ["HS256"],
});

       const hotelOwner = async (req, res, next) => {
  let Hotel = await hotel.findById(req.params.hotelId).exec()
  let owner = Hotel.postedBy._id.toString() === req.user._id.toString();
  if (!owner) {
    return res.status(403).send("Unauthorized");
  }
  next();
};